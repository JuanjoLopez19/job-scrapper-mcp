{
  "name": "job-scrapper-mcp-workspace",
  "private": true,
  "packageManager": "pnpm@12.6.0",
  "version": "1.1.0",
  "scripts": {
    "prepare": "husky",
    "web:dev": "pnpm --dir website dev",
    "web:build": "pnpm --dir website build",
    "web:check": "pnpm --dir website check"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}